import 'package:flutter/material.dart';

class DjokerNole extends StatelessWidget {
  const DjokerNole({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      'assets/images/djock.jpg',
    );
  }
}
